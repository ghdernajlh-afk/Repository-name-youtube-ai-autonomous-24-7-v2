# تشغيل 24/7

## Windows
للتشغيل المستمر استخدم Task Scheduler وشغّل `run.bat` عند تسجيل الدخول/بدء التشغيل.

## Linux
استخدم systemd أو Docker Compose مع restart: unless-stopped.

## Docker
1. `cp .env.example .env`
2. ضع OAuth JSON في credentials/client_secret.json
3. `docker compose up -d --build`

## حدود الأمان
الوكيل ينشئ بحد يومي `DAILY_JOB_LIMIT` (افتراضياً 2).
لا يرفع الفيديو تلقائياً من مهمة البحث؛ الرفع خطوة منفصلة في Dashboard.
لا يفعّل Public تلقائياً إلا إذا غيرت AUTO_PUBLISH=true.

## تحسينات لاحقة اختيارية
- مزود صور AI حقيقي لكل مشهد.
- مزود TTS مدفوع.
- YouTube Analytics API متقدم.
- جدولة نشر دقيقة.
- مراقبة التعليقات.
